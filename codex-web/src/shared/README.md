# Shared frontend modules

Use this directory for hand-written frontend UI primitives, formatting helpers, and browser-only utilities that are reused across product features.

Cross-boundary message and data types used by both the browser and server still live in the package-root `shared.ts`.
