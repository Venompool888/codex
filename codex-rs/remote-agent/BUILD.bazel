load("//:defs.bzl", "codex_rust_crate")

codex_rust_crate(
    name = "remote-agent-lib",
    compile_data = glob(["static/**"]),
    crate_name = "codex_remote_agent",
)

test_suite(
    name = "remote-agent",
    tests = [
        ":remote-agent-lib-api-test",
        ":remote-agent-lib-unit-tests",
    ],
)
